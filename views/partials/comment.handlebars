{{! partials for comments }}
<section class="comments">
	<div class="comments-card">
		<p> {{comment.content}} </p>
	</div>
	<div class="comment-user">
		<p> {{username}}, {{format_date date_created}}</p>
	</div>
</section>